# NgComments

Мелкое приложение для отображения древовидных комментов

## Запуск

Перед запуском стоит заполнить базу тестовыми комментами, а значит перейти в `/backend` и потом

```
npm ci
npm run populate
```

а потом просто `npm ci && npm run start:all` в корне
